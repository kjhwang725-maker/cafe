# 이미지 URL ?v= 값의 앞부분(접두)만 적습니다. 실행할 때마다 KST 날짜·시간이 자동으로 붙어
# 브라우저 캐시 완화에 도움이 됩니다. jsDelivr CDN 은 ?v= 로는 안 바뀌므로, 카페 반영은
# cafe_door.bat 이 커밋 후 …/gh/…@커밋SHA/…/ticker.png 로 덮어씁니다.
# 같은 날 여러 번 올릴 때만 숫자를 올리세요 (1 → 2 …).
# generate_dashboard 실행 시 cafe-door.html·data.json 에 반영됩니다.
1
